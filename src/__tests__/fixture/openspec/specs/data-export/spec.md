## ADDED Requirements

### Requirement: User can export data as CSV
The system SHALL allow users to download their data in CSV format.

#### Scenario: Export triggered
- **WHEN** a user clicks Export
- **THEN** a CSV file is downloaded
